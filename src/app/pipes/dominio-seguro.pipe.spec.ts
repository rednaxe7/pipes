import { DominioSeguroPipe } from './dominio-seguro.pipe';

describe('DominioSeguroPipe', () => {
  it('create an instance', () => {
    const pipe = new DominioSeguroPipe();
    expect(pipe).toBeTruthy();
  });
});
